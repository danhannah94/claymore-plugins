---
description: Reply to unresolved Foundry annotations on a document.
argument-hint: "[doc path]"
---

Reply to annotations: **$ARGUMENTS**

Run the `lean-reply` skill. Read all unresolved annotations on the target
document, group by section, and post thoughtful replies to each thread.

Does NOT resolve annotations or edit the document — that's what /update does.
