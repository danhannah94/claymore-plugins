---
description: Execute stories with sub-agents — work, review, wait for approval before merging
argument-hint: "<epic doc path or 'next'>"
---

Execute the stories from: **$ARGUMENTS**

Invoke the `csdlc-execute` skill. Work each story with a sub-agent,
review the PR, and wait for your approval before merging. Supervised
execution — you stay in the loop.
