---
description: Run all stories in an epic autonomously — Hurricane Pattern. Full send.
argument-hint: "<epic doc path>"
---

Ship this epic: **$ARGUMENTS**

Invoke the `csdlc-shipit` skill. Read all stories from the epic doc,
build the dependency graph, dispatch in batches, review and merge,
report back. The Hurricane Pattern — full autonomous execution with
quality gates between batches.
