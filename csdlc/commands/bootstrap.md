---
description: Set up CSDLC for a project — generate WORKFLOW.md, create Foundry project space, scaffold design doc
argument-hint: "[project name]"
---

Bootstrap CSDLC for: **$ARGUMENTS**

Invoke the `csdlc-bootstrap` skill. Set up the CSDLC methodology for an
existing or new project. Generates WORKFLOW.md, creates a Foundry project
space, and scaffolds a design doc.

If no project name was provided, ask what project we're setting up.
